fname := main

cat:
	cat Makefile

c:
	dmd -de -w ${fname}.d

cr:
	dmd -de -w -run ${fname}.d

r:
	./${fname}

release:
	dmd -release -O ${fname}.d

test:
	dmd -unittest -w -checkaction=context -run ${fname}.d

clean:
	rm ${fname}
